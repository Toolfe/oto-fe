import { ResWithLoadingPipe } from './res-with-loading.pipe';

describe('ResWithLoadingPipe', () => {
  it('create an instance', () => {
    const pipe = new ResWithLoadingPipe();
    expect(pipe).toBeTruthy();
  });
});
