import { SkeletonLoadingDirective } from './skeleton-loading.directive';

describe('SkeletonLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new SkeletonLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
