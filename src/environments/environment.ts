// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  AUTH_URL: 'http://localhost:20001/',
  ORG_URL: 'http://localhost:20011/',
  ROLE_URL: 'http://localhost:20021/',
  EMP_URL: 'http://localhost:20031/',
  CONTACT_URL: 'http://localhost:20041/',
  PROJECT_URL: 'http://localhost:20051/',
  DOC_URL: 'http://localhost:20061/',
  TASK_URL: 'http://localhost:20071/',
  TMS_URL: 'http://localhost:20081/',
  DMS_URL: 'http://localhost:20091/'
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
